# 📝 Как заполнить .env файл

## ✅ Шаги по порядку:

### 1. **DATABASE_URL** (Supabase)
```
Где взять: Supabase Dashboard → Settings → Database → Connection string → URI
Что сделать: Скопировать строку и заменить [YOUR-PASSWORD] на реальный пароль
Пример: postgresql://postgres.xxxxx:пароль@aws-0-eu-central-1.pooler.supabase.com:5432/postgres
```

### 2. **NEXTAUTH_SECRET и JWT_SECRET**
```
Как сгенерировать: В терминале выполни:
openssl rand -base64 32

Или: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

Что сделать: Скопировать результат в оба поля
```

### 3. **NEXTAUTH_URL**
```
Оставить как есть: https://boardly.online
```

### 4. **RESEND_API_KEY**
```
Где взять: Resend Dashboard → API Keys → Create API Key
Что скопировать: Весь ключ (начинается с re_)
```

### 5. **EMAIL_FROM**
```
Оставить как есть: Boardly <noreply@boardly.online>
```

### 6. **NEXT_PUBLIC_SOCKET_URL**
```
Получить после деплоя на Render
Будет: https://boardly-websocket.onrender.com
```

### 7. **Supabase Public** (опционально)
```
NEXT_PUBLIC_SUPABASE_URL: Supabase Dashboard → Settings → API → Project URL
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY: Supabase Dashboard → Settings → API → anon public key
```

---

## 🔧 Проверка заполнения

После заполнения всех полей:

### Локально протестировать:
```bash
# Проверить что все переменные загружаются
npm run dev

# Если есть ошибки - проверить синтаксис .env файла
```

### Для деплоя:
- **Vercel**: Скопировать переменные из .env в Vercel Dashboard
- **Render**: Скопировать переменные из .env в Render Dashboard

---

## ⚠️ Важно!

- **Не коммить .env файл в git!** (он уже в .gitignore)
- **Все секреты должны быть минимум 32 символа**
- **DATABASE_URL должен быть одинаковый везде**
- **NEXT_PUBLIC_SOCKET_URL добавить после деплоя Render**

---

## 🎯 Следующие шаги:

1. Заполни .env файл
2. Протестируй локально: `npm run dev`
3. Следуй инструкции из `ИНСТРУКЦИЯ_ПО_ДЕПЛОЮ.md`
4. Копируй переменные в Vercel и Render

---

**Готов? Открой `ИНСТРУКЦИЯ_ПО_ДЕПЛОЮ.md` и начинай деплой! 🚀**