name: Close Issues On Develop Merge

on:
  pull_request_target:
    types:
      - closed
    branches:
      - develop

concurrency:
  group: close-issues-on-develop-merge-${{ github.event.pull_request.number }}
  cancel-in-progress: false

jobs:
  close-linked-issues:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
      issues: write
      pull-requests: read
    steps:
      # pull_request_target runs the base branch's workflow, so this checks out
      # develop, never the PR head. Needed for the matcher module below.
      - name: Checkout base branch
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Close linked issues from PR body
        uses: actions/github-script@v7
        with:
          script: |
            const { parseClosingReferences } = require('./.github/scripts/parse-closing-references.js')

            const pr = context.payload.pull_request
            const owner = context.repo.owner
            const repo = context.repo.repo

            const issueNumbers = parseClosingReferences(pr.body, { owner, repo })

            if (issueNumbers.length === 0) {
              core.info('No same-repository closing keywords found in the merged PR body.')
              return
            }

            for (const issue_number of issueNumbers) {
              const { data: issue } = await github.rest.issues.get({
                owner,
                repo,
                issue_number,
              })

              if (issue.pull_request) {
                core.info(`Skipping #${issue_number} because it is a pull request, not an issue.`)
                continue
              }

              if (issue.state === 'closed') {
                core.info(`Skipping #${issue_number} because it is already closed.`)
                continue
              }

              await github.rest.issues.update({
                owner,
                repo,
                issue_number,
                state: 'closed',
              })

              const commentLines = [
                `Closed automatically after PR #${pr.number} was merged into \`${pr.base.ref}\`.`,
                '',
                `PR: ${pr.html_url}`,
              ]

              if (pr.merge_commit_sha) {
                commentLines.push(`Merge commit: ${pr.merge_commit_sha}`)
              }

              await github.rest.issues.createComment({
                owner,
                repo,
                issue_number,
                body: commentLines.join('\n'),
              })
            }
